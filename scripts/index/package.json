{
  "name": "job-triage-index",
  "private": true,
  "description": "Crawler and retriever for the shared job index. Scoped here on purpose: the app in index.html has no build step and no dependencies, and must keep it (see CLAUDE.md). Nothing under scripts/ is loaded by the browser. The .js files are CommonJS like the rest of scripts/; embed.mjs is ESM because transformers.js ships ESM only.",
  "scripts": {
    "embed": "node embed.mjs"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
